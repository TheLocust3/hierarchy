import styled from '@emotion/styled';

import { colors } from '../../../constants';

const HourDivider = styled('div')`
  border: 1px solid ${colors.lightestBlack};
  border-left: 0px;
  border-right: 0px;
  border-bottom: 0px;
`;

export default HourDivider;
